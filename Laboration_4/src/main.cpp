#include "Prototypes.h"
#include "memstat.hpp"



int main() {
    /*assignmentFunction();

    // Example usage of <chrono>
    auto timeStart = std::chrono::high_resolution_clock::now();
    std::cout << "Tryck ENTER!!" << std::endl;
    std::string s;
    while(!getline(std::cin, s)) {}        // wait for the user to press ENTER
    auto timeEnd = std::chrono::high_resolution_clock::now();

    std::cout << std::fixed << std::setprecision(6);

    // calculate duration by subtracting start time from end time
    size_t duration = std::chrono::duration_cast<std::chrono::milliseconds>(timeEnd - timeStart).count();
    double seconds = 1.0 * duration / 1000;

    std::cout << "Duration = "
         << seconds
         << " [s]" <<  std::endl;*/

    //IntArray intArray(10);
    /*cout << "quick sort 1" << endl;
    intArray.fillIntArray();
    intArray.printArray();
    intArray.quick1();
    intArray.printArray();

    cout << "quick sort 2" << endl;
    intArray.fillIntArray();
    intArray.printArray();
    intArray.quick2();
    intArray.printArray();*/

   /* cout << "selection sort" << endl;
    intArray.fillIntArray();
    intArray.printArray();
    intArray.selectionSort();
    intArray.printArray();

    cout << "insertion sort" << endl;
    intArray.fillIntArray();
    intArray.printArray();
    intArray.insertionSort();
    intArray.printArray();

    cout << "bubble sort1" << endl;
    intArray.fillIntArray();
    intArray.printArray();
    intArray.bubbleSort1();
    intArray.printArray();

    cout << "bubble sort2" << endl;
    intArray.fillIntArray();
    intArray.printArray();
    intArray.bubbleSort2();
    intArray.printArray();*/

    /*IntArray intArray5000(5000);
    Sort sort5000(intArray5000);
    sort5000.selectionAlgorithm();
    sort5000.insertionAlgorithm();
    sort5000.bubbleAlgorithm1();
    sort5000.bubbleAlgorithm2();
    sort5000.quickAlgorithm2();*/

    Program().run();
    return 0;
}
